import XCTest
@testable import FiniteEnumModule

final class FiniteEnumModuleTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(FiniteEnumModule().text, "Hello, World!")
    }
}
